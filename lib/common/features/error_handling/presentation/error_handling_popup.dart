import 'package:flutter/material.dart';

// TODO: make dialog shaklo 7lw
class ErrorHandlingPopup extends StatelessWidget {
  const ErrorHandlingPopup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Dialog();
  }
}
